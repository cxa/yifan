• Photo uploads on Android no longer fail for large camera photos — the app now compresses and resizes them the same way iOS does
• Post a photo without typing anything — the Post button now unlocks as soon as you pick an image
• Image-only posts look tighter, and thumbnail corners flow better with the card shape
• Faster, leaner photo uploads on both platforms — the image is streamed from disk instead of round-tripped through base64
